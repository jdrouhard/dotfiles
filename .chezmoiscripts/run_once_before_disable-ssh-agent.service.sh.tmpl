{{- if and .os.linux (ne .chezmoi.username "root") -}}
#!/bin/bash

if systemctl --user list-unit-files ssh-agent.service &>/dev/null; then
  systemctl --user disable ssh-agent.service || exit 0
fi

{{- end -}}

